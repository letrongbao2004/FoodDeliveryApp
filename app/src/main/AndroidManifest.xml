<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".FoodDeliveryApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodDeliveryApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Splash -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.FoodDeliveryApp.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Customer Auth -->
        <activity
            android:name=".activities.AuthActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Customer Main (Home + BottomNav) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="false" />

        <!-- Restaurant Detail -->
        <activity
            android:name=".activities.RestaurantDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Cart -->
        <activity
            android:name=".activities.CartActivity"
            android:exported="false"
            android:parentActivityName=".activities.MainActivity" />

        <!-- Checkout -->
        <activity
            android:name=".activities.CheckoutActivity"
            android:exported="false"
            android:parentActivityName=".activities.CartActivity" />

        <!-- Order Tracking -->
        <activity
            android:name=".activities.OrderTrackingActivity"
            android:exported="false" />

        <!-- Order Detail -->
        <activity
            android:name=".activities.OrderDetailActivity"
            android:exported="false"
            android:label="Order Detail"
            android:parentActivityName=".activities.MainActivity" />

        <!-- MoMo Payment -->
        <activity
            android:name=".activities.PaymentActivity"
            android:exported="false"
            android:label="MoMo Payment" />

        <!-- Merchant Main -->
        <activity
            android:name=".activities.MerchantMainActivity"
            android:exported="false" />

        <activity
            android:name=".activities.MerchantRestaurantSetupActivity"
            android:exported="false"
            android:parentActivityName=".activities.MerchantMainActivity" />

        <activity
            android:name=".activities.MerchantRestaurantDetailActivity"
            android:exported="false"
            android:parentActivityName=".activities.MerchantMainActivity" />

        <activity
            android:name=".activities.MerchantDashboardActivity"
            android:exported="false"
            android:parentActivityName=".activities.MerchantRestaurantDetailActivity" />

        <activity
            android:name=".activities.MerchantOrderHistoryActivity"
            android:exported="false"
            android:parentActivityName=".activities.MerchantRestaurantDetailActivity" />

        <activity
            android:name=".activities.ChatDetailActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".activities.MainActivity" />

        <activity
            android:name=".activities.MerchantChatListActivity"
            android:exported="false"
            android:parentActivityName=".activities.MerchantRestaurantDetailActivity" />

    </application>

</manifest>